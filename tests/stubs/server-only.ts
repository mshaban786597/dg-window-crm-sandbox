/**
 * Vitest stub for the `server-only` guard package.
 * The real module throws when imported outside a Server Component; that guard
 * protects the client bundle, not the test runner.
 */
export {};
